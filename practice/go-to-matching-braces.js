/**
 * Demo changing surrounding tag. Useful for html, xml, jsx.
 * Press: cst<tagname>  || e.g cst<span>
 * vscode-vim.muhajirframe.com
 */

const somefunction = (param1, param2, param3) => {
  console.log("Go to matching brace in action")
}
